{% extends 'base.html.twig' %}

{% block title %}Document{% endblock %}

{% block body %}
    <h1>Document</h1>
       <main id="main" class="main">
    <div class="pagetitle">
      <h1>Documents enregistrer</h1>
      <nav>
        <ol class="breadcrumb">
          <!--<li class="breadcrumb-item"><a href=""></a></li>
          <li class="breadcrumb-item"></li>
          <li class="breadcrumb-item active"></li>-->
        </ol>
      </nav>
    </div><!-- End Page Title -->
    </div><!-- End Logo --> <br>
    <section class="section">
      <div class="row">
        <div class="col-lg-12">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"></h5>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ document.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ document.type }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ document.objet }}</td>
            </tr>
            <tr>
                <th>Provenance</th>
                <td>{{ document.provenance }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ document.file }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ document.date ? document.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
     </div>
        </div>
    </section>
    <div class="d-flex justify-content">
    <a  class="btn btn-success btn-sm" href="{{ path('app_document_index') }}">back to list</a>.
    {{ include('document/_delete_form.html.twig') }}
</div>
</main><!-- End #main -->
    </div>
{% endblock %}
