<form method="post" action="{{ path('app_doc_lettre_delete', {'id': doc_lettre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doc_lettre.id) }}">
    <button class="btn">Delete</button>
</form>
